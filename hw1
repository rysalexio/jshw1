<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <script>
        //первое задание//
        let Tc = 23;
        let Tf = (9 / 5) * Tc + 32;
        alert(Tf);

        //второе задание//
        let name = 'Василий';
        let admin = name;
        console.log(admin);

        //третье задание//
        console.log(10 + 10 + "10");
        console.log(10 + "10" + 10);
        console.log(10 + 10 + +"10");
        console.log(10 / -"");
        console.log(10 / +"2,5");
        let result = 3 - 4 * 2;
        console.log(result);
        /*
        1. мы сначала сложили два Числа 10, произвели конкатенацию т.к. следующее значение является строкой, а потом склеили со Строкой "10"
        2. первое и последнее Число 10 произвели конкатенацию т.к. соседние к ним значение является строкой, и склеили три строки "10"
        3. мы сложили первые два Числа 10, Строку "10" привели к числовому значению через унарный плюс и сложили с пред. значением
        4. Число 10 разделили на -null, что по сути является делением на -0
        5. Число 10 разделили на строку т.к. что бы привести данную строку в число его надо записать как "2.5"
        6. сначала по приоритету выполняется умножение, потом вычетание
        */
    </script>
</body>

</html>